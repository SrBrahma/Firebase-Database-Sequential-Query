# Firebase-Database-Sequential-Query
npm package for Firebase Realtime Database to fetch and store live and on demand timestamped data. Useful for messages apps or a log system. React-native only for now.
